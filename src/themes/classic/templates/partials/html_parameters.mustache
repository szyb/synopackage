            <li class="parameters">
                {{#TotalPackages}}
                <div class="param-title">List of packages ({{TotalPackages}}) are valid for parameters:</div>
                {{/TotalPackages}}
                {{^TotalPackages}}
                <div class="param-title">List of packages are valid for parameters:</div>
                {{/TotalPackages}}
                <div class="table">
                    {{#sourceUrl}}
                    <div class="table-row">
                        <div class="table-cell"><span class="param-label">Source:</span></div>
                        <div class="table-cell"><span class="param-param">{{sourceUrl}}</span></div>
                        <div class="table-cell"><span class="param-param"><a href="{{sourceWWW}}" alt="visit website" target="_blank">Visit website</a></span></div>
                    </div>
                    {{/sourceUrl}}
                    <div class="table-row">
                        <div class="table-cell"><span class="param-label">Model:</span></div>
                        <div class="table-cell"><span class="param-param">{{ModelName}}</span></div>
                    </div>
                    <div class="table-row">
                        <div class="table-cell"><span class="param-label">Arch:</span></div>
                        <div class="table-cell"><span class="param-param">{{Arch}}</span></div>
                    </div>
                    <div class="table-row">
                        <div class="table-cell"><span class="param-label">DSM Version:</span></div>
                        <div class="table-cell"><span class="param-param">{{DSMVersion}}</span></div>
                    </div>
                    <div class="table-row">
                        <div class="table-cell"><span class="param-label">Include beta:</span></div>
                        <div class="table-cell"><span class="param-param">{{isBetaChecked}}</span></div>
                    </div>
                </div>
            </li>