site:
    testingOnly: false
    name: DSM Package Search
    theme: classic
    latestVersion: 6.1.3-15152
    defaultModel: DS216j
    websiteVersion: 0.8.4
    useCache: true
    #in strtotime format
    cacheExpiration: -1 day
    cachePngExpiration: -30 days
    logExecutionTime: true
    logSearchQueries: true
    #curl options:
    #in seconds
    curlTimeout: 15
    #leave empty if no proxy
    curlProxy: 
    #google analytics code (leave empty if not used)
    googleStatisticsCode: 
    
# ALL PATHS MUST BE RELATIVE TO THE ROOT
# DIRECTORY (where the index.php is located)
# INCLUDE TRAILING SLASH FOR DIRECTORIES!
paths:
    cache: cache/
    sourceUrls: config-files/badSource_urls.yaml
    models: config-files/badSynology_models.yaml
    versions: config-files/badDSMVersions.yaml
    changelog: config-files/badChangelog.yaml
    news: config-files/badNews.yaml
    faq: config-files/badFaq.yaml
    monologDebug: logs/debug.log
    monologError: logs/error.log
relativePaths:
    themes: themes/
    cache: cache/
